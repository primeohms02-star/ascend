import { auth } from "@clerk/nextjs/server";
import { redirect } from "next/navigation";
import { Award, CheckCircle2 } from "lucide-react";
import AppShell from "@/app/components/navigation/AppShell";
import { listUserProjectEvidence } from "@/lib/projects/service";

export default async function ProjectEvidencePage(){const{userId}=await auth();if(!userId)redirect("/sign-in");const evidence=await listUserProjectEvidence(userId);return <AppShell><main className="min-h-screen bg-gradient-to-br from-[#020617] via-[#08111f] to-[#0f172a]"><div className="mx-auto max-w-6xl px-5 py-7 sm:px-6 sm:py-9"><p className="text-[11px] font-semibold uppercase tracking-[0.2em] text-emerald-300">Projects</p><h1 className="mt-1 text-3xl font-bold text-white">Verified experience</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-400">Work you complete through Projects becomes evidence you can return to and use responsibly.</p>{evidence.length?<div className="mt-6 grid gap-4 md:grid-cols-2">{evidence.map((item)=><article key={item.id} className="rounded-2xl border border-emerald-300/12 bg-emerald-300/[0.035] p-5"><div className="flex items-start justify-between gap-3"><CheckCircle2 className="text-emerald-300"/><span className="rounded-full bg-emerald-300/[0.08] px-2.5 py-1 text-[10px] font-semibold uppercase tracking-[0.12em] text-emerald-200">{item.evidence_status}</span></div><h2 className="mt-4 text-lg font-semibold text-white">{item.title}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{item.summary}</p><div className="mt-4 flex flex-wrap gap-2">{(item.skills as string[]).map((skill)=><span key={skill} className="rounded-lg bg-white/[0.05] px-2.5 py-1 text-xs text-slate-300">{skill}</span>)}</div><p className="mt-4 text-xs text-slate-500">Visibility: {item.visibility.replaceAll("_"," ")}</p></article>)}</div>:<div className="mt-8 rounded-2xl border border-dashed border-white/10 p-10 text-center"><Award className="mx-auto text-slate-600"/><p className="mt-4 font-semibold text-white">Your evidence will appear here</p><p className="mt-2 text-sm text-slate-500">Complete a Project and pass review to create your first evidence record.</p></div>}</div></main></AppShell>}
